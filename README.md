Oracle ETL - S1
───────────────
Reads data from Oracle FREEPDB1 using Pandas.

- create_customers_csv.py — generates 100 customers
- insert_data.py — loads CSV into Oracle  
- test_db.py — reads from Oracle, exports to CSV
